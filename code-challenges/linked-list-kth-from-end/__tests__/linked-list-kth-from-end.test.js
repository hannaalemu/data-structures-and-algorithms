// 'use strict';

// const LinkedList = require('../linked-list');

// const linkedList = new LinkedList();

// describe('test linked list methods', () => {
//    it('can insert() a node at the head', () => {

//    }) 

//    it('can use includes() to see if the value is in the linked list', () => {

//    })

//    it('can use append(value) to add new node to the end of the linked list', () => {
    
//    })
//    it('can use insertBefore(value, newValue) to add a node with the value of newValue before the node with data of value', () => {

//    })

//    it('can use insertAfter(value, newValue) to add a new node with the value of newValue after the node containing value', () => {

//    })

// });